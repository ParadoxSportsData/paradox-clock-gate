# MCP config — contains credentials, never commit
.mcp.json

# Game data — generated by scripts/fetch_games.py, not committed to repo
/data/

# Build output — root-anchored so it doesn't match cmd/clock-gate/
/clock-gate

# Test artifacts
/tmp/clock-gate-test-output.txt

# UI frontend — lives in ParadoxSportsData/paradox-ui (separate repo)
/ui/
