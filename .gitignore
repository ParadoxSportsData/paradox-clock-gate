# MCP config — contains credentials, never commit
.mcp.json

# Build output — root-anchored so it doesn't match cmd/clock-gate/
/clock-gate

# Test artifacts
/tmp/clock-gate-test-output.txt

# Frontend — node_modules must not be committed; src/ must not be excluded
ui/node_modules/
ui/dist/
ui/.env.development.local
